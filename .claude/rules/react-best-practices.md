# React Best Practices (2025–2026) – Enforce Strictly

- 'use server' / 'use client' directives correct
- Server components first; client only when interactive
- No useEffect for fetching → use Query
- Strict typing + exhaustive deps
- Avoid prop drilling → Zustand/Query
