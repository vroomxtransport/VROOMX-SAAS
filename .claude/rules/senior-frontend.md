# Senior Frontend Rules – React/Next.js

- Server components/actions default
- TanStack Query: staleTime 30s, realtime invalidation + cleanup
- Zustand: persist + JSON storage
- Strict TS, no 'any', exhaustive deps
- Memoization where renders are expensive