/**
 * @deprecated Use `@/lib/consent-text` directly — this file is a compatibility
 * shim and will be removed in a future cleanup.
 *
 * The canonical source is now `src/lib/consent-text.ts`, which is safe to
 * import from both server and client components.
 */
export * from '@/lib/consent-text'
